syntax = "proto3";

package proto;

option go_package="/proto";

//payload spec
//request & response
message AddRequest{
    int64 x = 1;
    int64 y = 2;
}

message AddResponse{
    int64 result = 1;
}


//service spec
service AppService{
    rpc Add(AddRequest) returns(AddResponse);
}